[tool.poetry]
name = "src"
version = "0.1.0"
description = "new mlops project"
authors = ["Alex Markin <alexnightmark@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
